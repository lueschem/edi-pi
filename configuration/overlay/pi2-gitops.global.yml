general:
    parameters:
        mender_device_type: pi2
        edi_bootstrap_architecture: armhf

playbooks:
    100_os_setup:
        parameters:
            kernel_image: linux-image-armmp-lpae
            enable_gitops: True
