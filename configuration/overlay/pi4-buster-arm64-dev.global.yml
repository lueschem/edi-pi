general:
    parameters:
        mender_device_type: pi4

bootstrap:
    architecture: arm64

playbooks:
    300_board_support:
        parameters:
            ebs_template: rpi4
            kernel_image: linux-image-4.19.93-v8+
    400_development_tools:
        parameters:
            install_development_tools: True

shared_folders:
    edi_workspace:
        folder: edi-workspace
        mountpoint: edi-workspace
