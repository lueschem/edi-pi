general:
    parameters:
        mender_device_type: pi3

bootstrap:
    architecture: arm64

playbooks:
    500_gitops:
        path: playbooks/gitops/main.yml

shared_folders:
    edi_workspace:
        folder: edi-workspace
        mountpoint: edi-workspace
