general:
    parameters:
        mender_device_type: pi3
        edi_bootstrap_architecture: arm64
        qemu_emulated_cpu: cortex-a57

playbooks:
    100_os_setup:
        parameters:
            enable_gitops: True
